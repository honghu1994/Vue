<!-- 
  案例：
    1.0 在页面上默认显示一个数据源
      1.1 数据源中默认有三个数据
      1.2 每条数据都占一定的高度
      1.3 这三条数据将来要在页面上占满一整个屏幕
    2.0 向上拉的过程中，如果触底，就动态再添加三级数据
 -->
<view class="box">
  <view class="item" wx:for="{{dataList}}" wx:key="{{ index }}">
    {{ item }}
  </view>
</view>